import { LCDClient, Wallet } from "@terra-money/terra.js";
export declare const getSigner: ({ network, signerId, keysPath, lcd, }: {
    network: string;
    signerId: string;
    keysPath: string;
    lcd: LCDClient;
}) => Wallet;
