import { AccAddress, LCDClient, Wallet } from "@terra-money/terra.js";
import { ContractConfig } from "../config";
declare type StoreCodeParams = {
    conf: ContractConfig;
    network: string;
    refsPath: string;
    lcd: LCDClient;
    noRebuild: boolean;
    contract: string;
    signer: Wallet;
    codeId?: number;
};
export declare const storeCode: ({ conf, noRebuild, contract, signer, network, refsPath, lcd, codeId, }: StoreCodeParams) => Promise<any>;
declare type InstantiateParams = {
    conf: ContractConfig;
    signer: Wallet;
    network: string;
    refsPath: string;
    lcd: LCDClient;
    admin?: AccAddress;
    contract: string;
    codeId: number;
    instanceId: string;
    sequence?: number;
};
export declare const instantiate: ({ conf, refsPath, network, lcd, signer, admin, contract, codeId, instanceId, sequence, }: InstantiateParams) => Promise<void>;
declare type MigrateParams = {
    conf: ContractConfig;
    signer: Wallet;
    contract: string;
    codeId: number;
    network: string;
    instanceId: string;
    refsPath: string;
    lcd: LCDClient;
};
export declare const migrate: ({ conf, refsPath, lcd, signer, contract, codeId, network, instanceId, }: MigrateParams) => Promise<void>;
export {};
