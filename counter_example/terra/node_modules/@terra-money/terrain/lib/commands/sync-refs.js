"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_ux_1 = require("cli-ux");
const fs = require("fs-extra");
class SyncRefs extends command_1.Command {
    async run() {
        const { flags } = this.parse(SyncRefs);
        cli_ux_1.cli.action.start(`syncing refs from '${flags["refs-path"]}' to '${flags.dest}`);
        fs.copyFileSync(flags["refs-path"], flags.dest);
        cli_ux_1.cli.action.stop();
    }
}
exports.default = SyncRefs;
SyncRefs.description = "Sync configuration with frontend app.";
SyncRefs.flags = {
    "refs-path": command_1.flags.string({ default: "./refs.terrain.json" }),
    dest: command_1.flags.string({ default: "./frontend/src/refs.terrain.json" }),
};
SyncRefs.args = [{ name: "file" }];
