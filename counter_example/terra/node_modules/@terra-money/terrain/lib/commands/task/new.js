"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const cli_ux_1 = require("cli-ux");
const fs_extra_1 = require("fs-extra");
const path = require("path");
class TaskNew extends command_1.Command {
    async run() {
        const { args, flags } = this.parse(TaskNew);
        cli_ux_1.cli.action.start(`Creating task: ${args.task}`);
        fs_extra_1.copyFileSync(path.join(__dirname, "..", "..", "template", "tasks", "template.js"), path.join(process.cwd(), "tasks", `${args.task}.js`));
        cli_ux_1.cli.action.stop();
    }
}
exports.default = TaskNew;
TaskNew.description = "create new task";
TaskNew.flags = {};
TaskNew.args = [{ name: "task" }];
